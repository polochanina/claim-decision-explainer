__pycache__/
*.py[cod]
.venv
.env
.pytest_cache/
*.egg-info/
catboost_info/
.deepeval/
.ipynb_checkpoints/

# regenerable local embedding cache (re-derived from issueDesc via the Voyage API)
issuedesc_emb_cache.json

# mlflow sqlite WAL temp files (mlflow.db itself IS committed)
mlflow.db-journal
mlflow.db-wal
mlflow.db-shm

# Claude Code harness runtime state, not project source
.claude/scheduled_tasks.lock
